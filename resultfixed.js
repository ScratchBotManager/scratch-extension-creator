class TestExtension { getInfo() { return { "id": "TestExtension", "name": "TestExtension" "blocks":[ {"opcode":"ifreporter", "blocktype":"reporter", "text":"If [if] then [then] else [else]", "arguments":{ "if": { "type": "boolean", "defaultValue":"false"},"then": { "type": "string", "defaultValue":"true"},"else": { "type": "string", "defaultValue":"false"},]}, "menus":{}}; ifreporter({if, then, else}) if (if.ifreporter == true) {return then.ifreporter;} else {return else.ifreporter;} }; } Scratch.extensions.register(new TestExtension());
